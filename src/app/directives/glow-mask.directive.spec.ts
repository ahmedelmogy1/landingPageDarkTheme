import { GlowMaskDirective } from './glow-mask.directive';

describe('GlowMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new GlowMaskDirective();
    expect(directive).toBeTruthy();
  });
});
