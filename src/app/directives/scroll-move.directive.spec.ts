import { ScrollMoveDirective } from './scroll-move.directive';

describe('ScrollMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollMoveDirective();
    expect(directive).toBeTruthy();
  });
});
